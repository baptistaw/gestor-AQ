{
  "name": "gestor-de-consentimiento-de-anestesia",
  "version": "1.0.0",
  "private": true,
  "description": "Anesthesia‑consent manager. Los comandos se lanzan desde esta raíz.",
  "scripts": {
    "install:backend": "npm install --prefix backend",

    "dev": "npm --prefix backend run dev",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run start",

    "db:seed": "npm --prefix backend run db:seed",
    "db:migrate:deploy": "npm --prefix backend run db:migrate:deploy",
    "db:migrate:dev": "npm --prefix backend run db:migrate:dev"
  },
  "dependencies": {
    "qrcode.react": "^4.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17"
  }
}
