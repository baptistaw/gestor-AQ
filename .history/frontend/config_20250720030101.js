/*  Este script se ejecuta como <script src="/config.js">             */
/*  y deja la configuración global que consume tu frontend React.     */

window.APP_CONFIG = {
  /** URL base del backend (ajusta el puerto si cambias) */
  API_BASE_URL: "http://localhost:4000/api"
};
